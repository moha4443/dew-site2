---
description: نشر الموقع على GoDaddy (Deploy to GoDaddy)
---

# خطوات نشر الموقع على GoDaddy

## المرحلة 1️⃣: بناء المشروع (Build)

// turbo
1. قم ببناء نسخة الإنتاج من المشروع:
```bash
npm run build
```

هذا الأمر سينشئ مجلد `dist` يحتوي على جميع ملفات الموقع الجاهزة للنشر.

---

## المرحلة 2️⃣: تجهيز ملفات النشر

2. تأكد من وجود مجلد `dist` بنجاح وأنه يحتوي على:
   - ملف `index.html`
   - مجلد `assets` (يحتوي على ملفات CSS و JS)
   - الصور وملفات الوسائط الأخرى

---

## المرحلة 3️⃣: اختيار طريقة النشر على GoDaddy

لديك **3 خيارات** لنشر الموقع على GoDaddy:

### **الخيار A: استخدام cPanel File Manager** (الأسهل)

3. سجّل الدخول إلى حساب GoDaddy الخاص بك
4. اذهب إلى **My Products** → **Web Hosting** → **cPanel**
5. افتح **File Manager**
6. انتقل إلى مجلد `public_html` (أو المجلد الرئيسي للموقع)
7. احذف أي ملفات موجودة في `public_html` (إذا كان جديدًا)
8. ارفع **جميع محتويات** مجلد `dist` (ليس المجلد نفسه، بل الملفات داخله):
   - `index.html`
   - مجلد `assets`
   - جميع الصور والملفات الأخرى
9. تأكد من أن ملف `index.html` موجود مباشرة في `public_html`

### **الخيار B: استخدام FTP**

3. حمّل برنامج FTP مثل **FileZilla** من: https://filezilla-project.org/
4. احصل على بيانات FTP من GoDaddy:
   - سجّل الدخول إلى GoDaddy
   - اذهب إلى **Web Hosting** → **Settings** → **FTP**
   - احصل على: **Host**, **Username**, **Password**
5. افتح FileZilla واتصل باستخدام بيانات FTP
6. في الجانب الأيسر (Local): انتقل إلى مجلد `dist` في جهازك
7. في الجانب الأيمن (Remote): انتقل إلى مجلد `public_html`
8. حدد **جميع الملفات** داخل مجلد `dist` (Ctrl+A)
9. اسحبها إلى مجلد `public_html` على الجانب الأيمن
10. انتظر حتى اكتمال الرفع

### **الخيار C: استخدام Git Deployment** (الأكثر احترافية)

3. سجّل الدخول إلى **cPanel** في GoDaddy
4. ابحث عن **Git Version Control** (إذا كان متوفرًا)
5. انقر على **Create** لإنشاء repository جديد
6. اربط repository الموجود على GitHub/GitLab
7. حدد branch و path (`/public_html`)
8. قم بـ **Pull/Deploy** لسحب الكود وتثبيته

---

## المرحلة 4️⃣: إعداد ملف .htaccess (مهم للـ React Router)

9. أنشئ ملف `.htaccess` في مجلد `public_html` بالمحتوى التالي:

```apache
<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /
  RewriteRule ^index\.html$ - [L]
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteCond %{REQUEST_FILENAME} !-l
  RewriteRule . /index.html [L]
</IfModule>
```

هذا الملف يضمن أن جميع الروابط (routes) في React Router تعمل بشكل صحيح.

---

## المرحلة 5️⃣: التحقق والاختبار

10. افتح المتصفح واذهب إلى نطاقك (domain):
    - مثال: `https://yourdomain.com`
11. اختبر جميع الصفحات والروابط للتأكد من عملها
12. تحقق من تحميل الصور و CSS بشكل صحيح
13. اختبر على أجهزة مختلفة (موبايل، تابلت، ديسكتوب)

---

## المرحلة 6️⃣: تفعيل SSL (HTTPS) ✅

14. في cPanel، اذهب إلى **SSL/TLS Status**
15. فعّل SSL للنطاق الخاص بك
16. انتظر بضع دقائق حتى يتم تفعيل الشهادة
17. تأكد من أن الموقع يعمل على `https://` بدلاً من `http://`

---

## المرحلة 7️⃣: تحسينات إضافية (اختياري)

### ضغط الملفات (Gzip Compression)
15. أضف إلى ملف `.htaccess`:
```apache
<IfModule mod_deflate.c>
  AddOutputFilterByType DEFLATE text/html text/plain text/xml text/css text/javascript application/javascript application/json
</IfModule>
```

### Cache Control
16. أضف إلى ملف `.htaccess`:
```apache
<IfModule mod_expires.c>
  ExpiresActive On
  ExpiresByType image/jpg "access plus 1 year"
  ExpiresByType image/jpeg "access plus 1 year"
  ExpiresByType image/gif "access plus 1 year"
  ExpiresByType image/png "access plus 1 year"
  ExpiresByType text/css "access plus 1 month"
  ExpiresByType application/javascript "access plus 1 month"
</IfModule>
```

---

## ملاحظات مهمة ⚠️

- **حجم الملفات**: تأكد من أن ملف الفيديو (`video.mov` - 906 MB) ضروري، لأنه كبير جدًا وقد يبطئ الموقع
- **التحديثات المستقبلية**: في كل مرة تحدث الموقع، قم بـ `npm run build` ثم ارفع ملفات `dist` الجديدة
- **النطاق**: تأكد من أن نطاقك (domain) مربوط بـ hosting على GoDaddy
- **DNS**: قد يستغرق نشر الموقع من 24-48 ساعة حتى ينتشر DNS عالميًا

---

## استكشاف الأخطاء 🔧

### إذا ظهرت صفحة بيضاء:
1. تحقق من Console في المتصفح (F12)
2. تأكد من رفع جميع الملفات من `dist`
3. تحقق من مسارات الملفات (paths)

### إذا لم تعمل الروابط (404 Error):
1. تأكد من إضافة ملف `.htaccess` الصحيح
2. تحقق من أن `mod_rewrite` مفعّل في cPanel

### إذا كانت الصور لا تظهر:
1. تحقق من رفع جميع الصور من مجلد `dist`
2. تحقق من permissions للملفات (يفضل 644)

---

## الخلاصة ✨

الموقع الآن جاهز للنشر! اتبع الخطوات أعلاه وستكون موقعك على الإنترنت في دقائق. 🚀

إذا واجهت أي مشكلة، راجع قسم استكشاف الأخطاء أو اتصل بدعم GoDaddy.
