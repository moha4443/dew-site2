# 🚀 نشر الموقع على GoDaddy - دليل سريع

## 📋 الخطوات الأساسية

### 1️⃣ بناء المشروع
```bash
npm run build
```
سيتم إنشاء مجلد `dist` يحتوي على جميع ملفات الموقع الجاهزة.

---

### 2️⃣ رفع الملفات على GoDaddy

#### الطريقة الأولى: cPanel File Manager (الأسهل)

1. **سجل الدخول** على GoDaddy
2. اذهب إلى: **My Products** → **Web Hosting** → **cPanel**
3. افتح **File Manager**
4. انتقل إلى مجلد `public_html`
5. احذف أي ملفات قديمة (إذا كانت موجودة)
6. ارفع **جميع محتويات** مجلد `dist` (ليس المجلد نفسه!)
   - `index.html`
   - مجلد `assets`
   - جميع الصور والملفات

⚠️ **مهم**: الملفات يجب أن تكون مباشرة في `public_html`، ليس في مجلد فرعي!

#### الطريقة الثانية: FTP

1. حمّل **FileZilla**: [https://filezilla-project.org](https://filezilla-project.org)
2. احصل على بيانات FTP من GoDaddy:
   - Web Hosting → Settings → FTP
3. اتصل بالـ FTP وارفع محتويات `dist` إلى `public_html`

---

### 3️⃣ إضافة ملف .htaccess

**مهم جداً لعمل React Router!**

أنشئ ملف `.htaccess` في مجلد `public_html` والصق فيه:

```apache
<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /
  RewriteRule ^index\.html$ - [L]
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteCond %{REQUEST_FILENAME} !-l
  RewriteRule . /index.html [L]
</IfModule>
```

💡 **نسخة جاهزة**: يوجد ملف `.htaccess` جاهز في جذر المشروع - فقط انسخه إلى `public_html`

---

### 4️⃣ تفعيل SSL (HTTPS)

1. في cPanel، اذهب إلى **SSL/TLS Status**
2. فعّل SSL للنطاق الخاص بك
3. انتظر 5-10 دقائق

---

### 5️⃣ الاختبار

افتح موقعك: `https://yourdomain.com`

اختبر:
- ✅ الصفحة الرئيسية
- ✅ جميع الروابط (Navigation)
- ✅ الصور والأيقونات
- ✅ الموبايل (Responsive Design)

---

## 🔧 حل المشاكل الشائعة

### صفحة بيضاء فارغة؟
- افتح Console (F12) لرؤية الأخطاء
- تأكد من رفع **جميع** الملفات من مجلد `dist`
- تحقق من مسارات الملفات

### روابط لا تعمل (404 Error)؟
- تأكد من إضافة ملف `.htaccess` الصحيح
- تحقق من أن `mod_rewrite` مفعّل في cPanel

### الصور لا تظهر؟
- تحقق من رفع جميع الصور من مجلد `dist`
- تحقق من permissions الملفات (يفضل 644)

---

## 📊 تحسينات الأداء

### ضغط ملف الفيديو الكبير
ملف `video.mov` حجمه **906 MB** - كبير جداً!

**خيارات بديلة:**
1. ضغط الفيديو باستخدام HandBrake أو FFmpeg
2. استخدام YouTube/Vimeo للاستضافة
3. تحويله إلى WebM (أصغر حجماً)

---

## 📚 ملفات إضافية

- **دليل مفصل**: `.agent/workflows/deploy-to-godaddy.md`
- **دليل سريع**: `DEPLOY-GUIDE.txt`
- **ملف .htaccess**: `.htaccess`

---

## 💡 نصائح

1. **النسخ الاحتياطي**: احتفظ بنسخة من موقعك القديم قبل الرفع
2. **التحديثات**: في كل مرة تحدّث الموقع → `npm run build` → ارفع `dist`
3. **DNS**: قد يستغرق النشر 24-48 ساعة للانتشار العالمي

---

## 🆘 الدعم

**GoDaddy Support**: [https://www.godaddy.com/help](https://www.godaddy.com/help)  
دعم عربي متاح 24/7

---

## 🎉 موقعك جاهز للانطلاق!

بعد اتباع الخطوات أعلاه، سيكون موقعك مباشراً على الإنترنت!
