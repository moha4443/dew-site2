# 🎬 دليل ضغط ملف الفيديو

## ⚠️ المشكلة
ملف `video.mov` حجمه **906 MB** (864 ميجابايت تقريباً)  
هذا حجم كبير جداً ويسبب:
- بطء شديد في تحميل الموقع
- استهلاك كبير للباندويدث
- تجربة مستخدم سيئة

---

## ✅ الحلول المتاحة

### الحل 1: ضغط الفيديو باستخدام FFmpeg (الأفضل)

#### تثبيت FFmpeg:
1. حمّل من: https://ffmpeg.org/download.html
2. أو استخدم: `choco install ffmpeg` (إذا كان لديك Chocolatey)

#### ضغط الفيديو:

```bash
# ضغط عادي (جودة عالية)
ffmpeg -i dist/video.mov -c:v libx264 -crf 23 -preset medium -c:a aac -b:a 128k dist/video-compressed.mp4

# ضغط أقوى (جودة متوسطة - حجم أصغر)
ffmpeg -i dist/video.mov -c:v libx264 -crf 28 -preset medium -c:a aac -b:a 96k dist/video-compressed.mp4

# ضغط قوي جداً (للويب)
ffmpeg -i dist/video.mov -c:v libx264 -crf 30 -preset fast -vf scale=1280:-2 -c:a aac -b:a 96k dist/video-web.mp4
```

**النتيجة المتوقعة**: تقليل الحجم إلى 50-100 MB (حسب الجودة)

---

### الحل 2: استخدام أدوات أونلاين

#### Clipchamp (مجاني):
https://clipchamp.com/en/video-compressor

#### HandBrake (برنامج مجاني):
https://handbrake.fr/downloads.php

**الخطوات:**
1. حمّل HandBrake
2. افتح `video.mov`
3. اختر Preset: **Web** → **Gmail Medium 5 Minutes 720p30**
4. اضغط **Start Encode**

---

### الحل 3: استخدام خدمات الاستضافة (الأسهل والأفضل!)

#### YouTube (الأفضل للفيديوهات الطويلة):
1. رفع الفيديو على YouTube
2. استخدم iframe للتضمين:
```html
<iframe 
  width="100%" 
  height="500" 
  src="https://www.youtube.com/embed/VIDEO_ID" 
  frameborder="0" 
  allowfullscreen>
</iframe>
```

#### Vimeo (أكثر احترافية):
1. رفع على Vimeo
2. استخدم iframe للتضمين

#### Cloudflare Stream:
- خدمة متخصصة في استضافة الفيديو
- سريعة جداً
- مدفوعة لكن بأسعار معقولة

---

## 🎯 التوصية

**للمواقع الاحترافية:**
- استخدم **YouTube** أو **Vimeo** (مجاني + سريع)
- لا حاجة لرفع الفيديو على السيرفر

**إذا كنت تريد استضافة محلية:**
1. اضغط الفيديو باستخدام FFmpeg
2. احذف `video.mov` الأصلي
3. استخدم `video-compressed.mp4` بدلاً منه

---

## 📝 تحديث الكود

إذا قمت بضغط الفيديو، حدّث المسار في الكود من:
```html
<video src="/video.mov">
```

إلى:
```html
<video src="/video-compressed.mp4">
```

أو استخدم YouTube:
```html
<iframe 
  src="https://www.youtube.com/embed/YOUR_VIDEO_ID"
  allowfullscreen>
</iframe>
```

---

## 🔢 مقارنة الأحجام

| الطريقة | الحجم المتوقع | الوقت اللازم |
|---------|---------------|--------------|
| الفيديو الأصلي | 906 MB | - |
| FFmpeg (عادي) | ~80-120 MB | 5-10 دقائق |
| FFmpeg (قوي) | ~30-60 MB | 5-10 دقائق |  
| YouTube/Vimeo | 0 MB (خارجي) | 15 دقيقة رفع |

---

## ⚡ الخلاصة

1. **الأفضل**: استخدم YouTube/Vimeo
2. **البديل**: اضغط الفيديو بـ FFmpeg
3. **تجنب**: رفع فيديو 906 MB على السيرفر مباشرة!
